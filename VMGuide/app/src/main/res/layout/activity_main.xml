<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.guide.MainActivity">

    <Button
        style="@style/mainactivity"
        android:id="@+id/faqs"
        android:text="FAQs"
        android:textSize="24dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:layout_centerVertical="true"
        android:background="@color/faqs"/>
    <Button
        style="@style/mainactivity"
        android:id="@+id/open_camera"
        android:layout_above="@id/faqs"
        android:text="Open Camera"
        android:textSize="24dp"
        android:background="@color/open_camera"/>
    <Button
        style="@style/mainactivity"
        android:id="@+id/recent_searches"
        android:layout_below="@id/faqs"
        android:text="Recent Searches"
        android:textSize="24dp"
        android:background="@color/recent_searches"/>

</RelativeLayout>
